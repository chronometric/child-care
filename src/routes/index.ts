/**
 * Single route configuration entry point.
 * - `AppRoutes.tsx` defines the `<Routes>` tree.
 * - `components/layout/AnimatedRoutes.tsx` re-exports the same component for the app shell.
 */
export { default } from "./AppRoutes";
export { default as AppRoutes } from "./AppRoutes";
