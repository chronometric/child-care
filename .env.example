# Copy to .env and fill in values. Never commit .env.

# Base URL of the child-care-backend API (no trailing slash required)
VITE_BACKEND_URL=http://localhost:8000

# Set to true to show auth payment / registration billing flows (Stripe etc.); default off for core care focus
# VITE_ENABLE_COMMERCIAL_FLOWS=true
